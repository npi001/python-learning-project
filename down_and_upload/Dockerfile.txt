FROM python:3.11-slim

# 设置工作目录
WORKDIR /app

# 复制并安装依赖（先复制 requirements 以利用缓存）
COPY requirements.txt /app/
RUN pip install --no-cache-dir -r requirements.txt

# 复制应用代码
COPY . /app

# 指定环境变量
ENV FLASK_ENV=production

# 暴露端口（仅说明用途）
EXPOSE 8080

# 容器启动命令
CMD ["python", "app.py"]